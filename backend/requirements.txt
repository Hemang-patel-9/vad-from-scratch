fastapi==0.141.1
librosa==0.11.0
numpy
# Runs the exported neural detector. CPU only - the model is ~1 M parameters and
# faster than realtime on one thread, so there is nothing here to put on a GPU.
onnxruntime==1.28.0
soundfile==0.14.0
uvicorn[standard]==0.52.1
