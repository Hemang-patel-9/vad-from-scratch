.git
.gitignore
docs
ml
LICENSE
README.md

frontend/node_modules
frontend/.next
frontend/out
frontend/README.md

**/__pycache__
**/*.pyc
**/.venv
**/venv

# Only the three files the backend loads belong in the image. The PyTorch
# checkpoint and the TorchScript copy are for retraining and re-exporting, and
# together they are larger than everything else in the build put together.
backend/models/*.pt
backend/models/*.ts
backend/models/*.bak
